# URL base de la API del backend
# En desarrollo no hace falta (el proxy de Vite apunta a localhost:8080)
# En producción apuntar a la URL del BE desplegado
VITE_API_URL=https://tu-backend.railway.app/api/v1
